#include <stdio.h>
#include <stdlib.h>

int main()
{
   int sum=0,num,rem;
   printf("Enter the number :");
   scanf("%d",&num);
   while(num!=0){
    rem=num%10;
    sum+=rem;
    num=num/10;
   }
   printf("Sum of digits : %d",sum);
    return 0;
}
